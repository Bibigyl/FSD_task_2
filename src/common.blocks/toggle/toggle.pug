include ../../common.blocks/title/title

mixin toggle({title, text, isChecked = false})
	if title
		.toggle__title
			+title(title)
	label.toggle__label
		input.toggle(type="checkbox" checked=isChecked)
		div.toggle__text= text
